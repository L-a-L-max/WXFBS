<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.wxchat.mapper.WeWorkConfigMapper">

    <resultMap type="com.demo.wxchat.domain.WeWorkConfig" id="WeWorkConfigResult">
        <result property="id" column="id"/>
        <result property="teamId" column="team_id"/>
        <result property="corpId" column="corp_id"/>
        <result property="agentId" column="agent_id"/>
        <result property="agentSecret" column="agent_secret"/>
        <result property="groupChatId" column="group_chat_id"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectByTeamId" parameterType="Long" resultMap="WeWorkConfigResult">
        select * from wework_config where team_id = #{teamId}
    </select>

    <insert id="insert" parameterType="com.demo.wxchat.domain.WeWorkConfig" useGeneratedKeys="true" keyProperty="id">
        insert into wework_config (team_id, corp_id, agent_id, agent_secret, group_chat_id, status, create_time)
        values (#{teamId}, #{corpId}, #{agentId}, #{agentSecret}, #{groupChatId}, #{status}, now())
    </insert>

    <update id="update" parameterType="com.demo.wxchat.domain.WeWorkConfig">
        update wework_config
        <set>
            <if test="corpId != null">corp_id = #{corpId},</if>
            <if test="agentId != null">agent_id = #{agentId},</if>
            <if test="agentSecret != null">agent_secret = #{agentSecret},</if>
            <if test="groupChatId != null">group_chat_id = #{groupChatId},</if>
            <if test="status != null">status = #{status},</if>
            update_time = now()
        </set>
        where id = #{id}
    </update>
</mapper>
